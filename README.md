# Survey Systems Pro: WinForms vs WPF Comparison

**Дисципліна:** Розробка інтерфейсів користувача  
**Виконав:** Яковлєва Єлизавета Дмитрівна, студентка 4-го курсу  
**Тема:** Лабораторна робота №1. Інструменти Windows Forms та Windows Presentation Foundation

---

##  Опис проєкту
Метою даної роботи є порівняльна реалізація багаторівневої системи опитування користувачів (6+ запитань) з використанням двох ключових технологій Microsoft .NET. Проєкт демонструє вміння працювати зі складними елементами керування, валідацією даних та файловими потоками.

###  Анкета включає:
1.  **Прізвище та ім'я** (Текстове поле з валідацією).
2.  **Вік** (Числовий лічильник / Слайдер).
3.  **Джерело інформації** (Випадаючий список ComboBox).
4.  **Оцінка складності/якості** (Група RadioButtons).
5.  **Цікаві модулі/теми** (Множинний вибір CheckBoxes).
6.  **Додаткові коментарі** (Багаторядкове поле введення).

---

##  Технологічні особливості

### 🟢 1. Версія Windows Forms 
Реалізована за принципом максимальної надійності та швидкодії.
- **Двигун:** GDI+ (Pixel-based rendering).
- **Особливість:** Використання LINQ для ефективного збору даних з групових контейнерів (`panelRating`, `groupTopics`).
- **Архітектура:** Подієво-орієнтований підхід.

### 🔵 2. Версія WPF 
Реалізована з акцентом на UX/UI дизайн та сучасні стандарти розробки.
- **Двигун:** DirectX (Vector-based rendering).
- **Data Binding:** Реалізована прив'язка даних у реальному часі для відображення значень слайдера.
- **Асинхронність:** Запис у файл виконується асинхронно (`async/await`, `Task.Run`), що запобігає блокуванню інтерфейсу користувача під час I/O операцій.
- **UX-фічі:** Використання `ScrollViewer` для адаптації під різні розміри екранів та градієнтна стилізація елементів.

---

##  Структура репозиторію
* `/SurveyApp` — проект на базі Windows Forms.
* `/ProfessionalSurveyPro` — проект на базі WPF.
* `survey_results.txt` — файл збережених результатів WinForms.
* `survey_wpf.txt` — файл збережених результатів WPF.

---

##  Порівняльна таблиця

| Характеристика | Windows Forms | WPF |
| :--- | :--- | :--- |
| **Рендеринг** | CPU (Растровий) | GPU (Векторний) |
| **Розмітка** | Designer (C# Code) | XAML (Декларативна) |
| **Масштабування** | Обмежене (High DPI issues) | Адаптивне (Vector) |
| **Потоки** | Переважно синхронно | Асинхронно (Async/Await) |

---

##  Як запустити
1. Відкрийте файл рішення `.sln` у **Visual Studio 2022**.
2. У Solution Explorer натисніть правою кнопкою на обраний проект (WinForms або WPF).
3. Виберіть **"Set as Startup Project"**.
4. Натисніть `F5` для запуску.

---

##  Ліцензія
Проєкт розроблено виключно в навчальних цілях для факультету комп'ютерної інженерії.
